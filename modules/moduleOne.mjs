const sum = (a, b) => a + b;
const mult = (a, b) => a * b;

export {sum, mult};

console.log(sum(22, 5));
console.log(mult(2, 5));

const one = 1;
const two = 'two';

export {
    one,
    two
};

console.log(one + two);